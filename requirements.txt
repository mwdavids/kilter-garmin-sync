# Runtime dependencies
boardlib>=0.9
fit-tool>=0.9.13
# BoardLib imports PIL (Pillow) at module load but doesn't declare it; pin it
# explicitly so a clean install / CI runner has it.
Pillow>=9.0
# Optional auto-upload to Garmin Connect (--upload / garmin-login).
garth>=0.4

# Dev/test (parse-back validation of generated FIT files, test runner)
garmin-fit-sdk>=21.115
pytest>=7.4
# Used by tests to validate the GitHub Actions workflow YAML parses.
PyYAML>=6.0
